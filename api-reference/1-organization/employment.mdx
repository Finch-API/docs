---
openapi: post /employer/employment
---
<RequestExample>
``` curl cURL
```

``` python Python
from finch import Finch

client = Finch(
    access_token="<access_token>",
)

page = client.hris.employments.retrieve_many(
    requests: [
        {
            "individual_id": "<individual_id>",
        }
    ]
)
print(page.page)
```

``` javascript Node
```

``` go Go
```

``` java Java
```

``` kotlin Kotlin
```
</RequestExample>